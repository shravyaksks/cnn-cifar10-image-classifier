# cnn-cifar10-image-classifier
CNN model trained on CIFAR‑10 dataset for image classification. Includes saved .keras model (20 epochs), training notebook, and sample inference code. Demonstrates deep learning with TensorFlow/Keras for recognizing 10 image classes.
